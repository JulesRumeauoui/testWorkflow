name: deploy
on: 
  workflow_dispatch:
    inputs:
      server:
        description: "Environement de déploiement"
        required: true
        default: "dev"


jobs:
  Premier_Job:
    name: Premier_Job
    runs-on: ubuntu-lastest
    steps:
      - run: echo ${{server}}

  
